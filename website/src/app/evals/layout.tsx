import type { Metadata } from "next";
import type { ReactNode } from "react";
import JsonLd from "@/components/JsonLd";

export const metadata: Metadata = {
  title: "Evals",
  description:
    "Transparent evaluation dashboard — 12 categories scored by LLM judges. Every score is public. Track how agents improve over time.",
  openGraph: {
    title: "Evaluation Dashboard",
    description:
      "Transparent AI agent evaluation across 12 categories. Every score is public.",
    type: "website",
  },
};

export default function EvalsLayout({
  children,
}: {
  children: ReactNode;
}) {
  return (
    <>
      <JsonLd
        data={{
          "@context": "https://schema.org",
          "@type": "Dataset",
          name: "Livestream to AGI Evaluation Data",
          description:
            "Public evaluation scores for 9 AI agents across 12 categories, generated by LLM judges.",
          url: "https://livestreamtoagi.com/evals",
          creator: {
            "@type": "Organization",
            name: "Livestream to AGI",
          },
        }}
      />
      {children}
    </>
  );
}
