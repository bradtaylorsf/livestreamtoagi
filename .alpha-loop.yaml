# Alpha Loop configuration
repo: bradtaylorsf/livestreamtoagi
project: 5
agent: claude
label: ready
base_branch: main
test_command: pnpm test:python && pnpm test:frontend && pnpm test:website
dev_command: pnpm dev
auto_merge: true
setup_command: "/opt/homebrew/bin/uv venv --python 3.13 .venv && /opt/homebrew/bin/uv pip install --python .venv/bin/python -r requirements.txt && npm ci --silent --prefix frontend && npm ci --silent --prefix website"
# Safety limits (0 = unlimited)
# batch: true
# batch_size: 2
max_issues: 25
max_session_duration: 18000  # 5 hours in seconds
agent_timeout: 7200  # 120 minutes in seconds

# === Added by alpha-loop init (new settings) ===
# These options were added in a newer alpha-loop release. They're commented out
# so your current behavior is unchanged — uncomment to opt in.

harnesses:
  - claude-code
#
