# Alpha Loop configuration
repo: bradtaylorsf/livestreamtoagi
project: 5
agent: codex
label: ready
base_branch: main
test_command: pnpm test
dev_command: pnpm dev
auto_merge: true
setup_command: "python3.13 -m venv .venv && .venv/bin/pip install -r requirements.txt"
# Safety limits (0 = unlimited)
max_issues: 0
max_session_duration: 7200  # 2 hours in seconds
