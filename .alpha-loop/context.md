Wrote `.claude/project-context.md` (~380 words). Refreshed from the prior version with verified counts (48 migrations, 19 repos), the actual list of admin route modules I saw on disk, and added the LLM-client routing rule and `make test-backend` as the PATH-safe test entry. Active State left as placeholders for the loop to fill.
