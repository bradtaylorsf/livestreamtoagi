# Agent Framework
crewai>=0.80.0
openai>=1.0.0  # OpenRouter uses OpenAI-compatible API

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0

# Database
asyncpg>=0.30.0
sqlalchemy[asyncio]>=2.0.0
pgvector>=0.3.0
alembic>=1.14.0

# Cache
redis>=5.0.0

# TTS
edge-tts>=6.1.0

# Observability
langfuse>=2.0.0

# YouTube auto-publish (optional, used by core/youtube/)
google-api-python-client>=2.100.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0

# Token Counting
tiktoken>=0.7.0

# Scheduling
apscheduler>=3.10.0,<4.0.0

# File watching
watchfiles>=0.21.0

# Utilities
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0

# Development
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
ruff>=0.8.0

# Video render (optional) — also installable via `pip install -e ".[render]"`
# After install, fetch browser binaries: `playwright install chromium`
# playwright>=1.47.0
